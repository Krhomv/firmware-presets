#$ TITLE: Krhom's Builds
#$ FIRMWARE_VERSION: 4.5
#$ FIRMWARE_VERSION: 4.4
#$ CATEGORY: OTHER
#$ STATUS: EXPERIMENTAL
#$ KEYWORDS: krhom, defaults, build, settings
#$ AUTHOR: krhomv

#$ DESCRIPTION: Easy new build setup

#$ OPTION_GROUP BEGIN: Rates

    #$ OPTION BEGIN (CHECKED): Set Rates
        profile 0
        rateprofile 0
        set thr_mid = 0
        set roll_rc_rate = 4
        set pitch_rc_rate = 4
        set yaw_rc_rate = 4
        set roll_expo = 40
        set pitch_expo = 40
        set yaw_expo = 40
    #$ OPTION END
    
#$ OPTION_GROUP END
